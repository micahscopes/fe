// For loop with #[no_unroll] should not unroll even for small arrays
pub fn for_array_no_unroll_sum() -> u64 {
    let arr: [u64; 3] = [1, 2, 3]
    let mut sum: u64 = 0
    #[no_unroll]
    for elem in arr {
        sum = sum + elem
    }
    sum
}
