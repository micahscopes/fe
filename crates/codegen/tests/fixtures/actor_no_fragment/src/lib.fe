// Reject fixture: a `GpuProgram` actor carrying the placement row but no
// `FragmentSurface` behavior, so there is nothing to serve as the render entry.

actor Bare uses (GpuProgram<i32>) {
    x: i32,
}
