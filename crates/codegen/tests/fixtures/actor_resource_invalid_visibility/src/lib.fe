use std::webgpu::{
    ActorResident,
    ComputeSurface,
    FixedDispatch,
    FragmentOnly,
    FragmentSurface,
    GpuBuffer,
    GpuProgram,
    ReadOnly,
    ReplayRecipe,
    StorageBuffer,
    StorageKind,
    WebGpuBackend,
    Workgroup,
    Zeroed,
}

pub type FragmentWords = GpuBuffer<
    StorageKind,
    ReadOnly,
    ActorResident,
    Zeroed,
    ReplayRecipe,
    FragmentOnly,
    1,
    u32,
>

actor InvalidVisibility uses (GpuProgram<WebGpuBackend>) {
    words: FragmentWords,
    sink: StorageBuffer<u32, 1>,

    fn inspect(self) uses (ComputeSurface<Workgroup<1, 1, 1>, FixedDispatch<1, 1, 1>>) {
        let observed = self.words.load(index: 0)
        self.sink.store(index: 0, value: observed)
    }

    fn paint(self, px: i32, py: i32) -> i32 uses (FragmentSurface) {
        self.words.load(index: 0).downcast_truncate()
    }
}
