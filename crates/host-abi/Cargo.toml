[package]
name = "fe-host-abi"
version = "0.1.0"
edition.workspace = true
license = "Apache-2.0"
description = "Target-neutral normalized host ABI model for Fe"

[dependencies]
serde = { version = "1.0", features = ["derive"] }

[dev-dependencies]
serde_json = "1"
