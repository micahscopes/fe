// Event acquisition has one explicit runtime-control authority. A consumer
// cannot silently construct or poll a second source outside a handler scope.

use std::reactive::subscribe_event

fn missing_event_source() {
    subscribe_event()
}
