[package]
name = "fe-host-runtime"
version = "0.1.0"
edition.workspace = true
license = "Apache-2.0"
description = "Platform-neutral runtime state machines for Fe host interfaces"

[dependencies]
fe-host-abi = { path = "../host-abi" }
serde = { version = "1.0", features = ["derive"] }

[dev-dependencies]
serde_json = "1"

