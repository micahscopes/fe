# Generated by the Fe compiler via:
#   cargo run -p fe-codegen --features native-backend --example gen_rollcall_evidence
# Regenerated per checkout; never hand-edited, never committed. Matches every
# other demos/*/gen/ convention in this repo (see demos/README.md).
gen/
